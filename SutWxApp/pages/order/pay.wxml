<!-- 支付页面结构 -->
<view class="container">
  <!-- 加载状态 -->
  <view class="loading" wx:if="{{loading}}">
    <text>加载中...</text>
  </view>
  
  <!-- 支付信息 -->
  <view class="pay-content" wx:else>
    <!-- 订单金额 -->
    <view class="amount-section">
      <text class="amount-label">支付金额</text>
      <text class="amount">¥{{orderInfo ? orderInfo.totalAmount.toFixed(2) : '0.00'}}</text>
    </view>
    
    <!-- 订单编号 -->
    <view class="order-info">
      <text>订单编号：{{orderInfo ? orderInfo.orderNo : ''}}</text>
    </view>
    
    <!-- 支付方式 -->
    <view class="payment-section">
      <view class="section-title">选择支付方式</view>
      
      <view class="payment-method" wx:for="{{paymentMethods}}" wx:key="id" wx:for-item="method" 
            bind:tap="onSelectPaymentMethod" data-id="{{method.id}}">
        <view class="method-left">
          <image class="method-icon" src="{{method.icon || '/images/default-post.svg'}}" mode="aspectFit"></image>
          <text class="method-name">{{method.name}}</text>
        </view>
        <view class="method-right">
          <view class="radio" wx:if="{{method.selected}}">
            <view class="radio-inner"></view>
          </view>
          <view class="radio radio-unselected" wx:else></view>
        </view>
      </view>
    </view>
    
    <!-- 支付按钮 -->
    <button class="pay-btn" bind:tap="onPay" loading="{{paying}}" disabled="{{paying}}">确认支付</button>
    
    <!-- 底部操作 -->
    <view class="bottom-actions">
      <text class="action-link" bind:tap="onViewOrderDetail">查看订单详情</text>
      <text class="action-link" bind:tap="onBackToOrderList">返回订单列表</text>
    </view>
  </view>
</view>