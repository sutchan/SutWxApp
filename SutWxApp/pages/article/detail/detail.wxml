<!--
 * 文件名: detail.wxml
 * 版本号: 1.0.1
 * 更新日期: 2025-11-24
 * 作者: Sut
 * 描述: 文章详情页面模板
-->
<!--pages/article/detail/detail.wxml-->
<view class="container">
  <view wx:if="{{loading}}" class="loading-tip">加载中...</view>
  
  <view wx:elif="{{error}}" class="error-container">
    <view class="error-icon">⚠️</view>
    <view class="error-message">{{error}}</view>
    <button class="retry-button" bindtap="handleRetry">重试</button>
  </view>
  
  <block wx:else>
    <view class="article-header">
      <view class="article-title">{{article.title}}</view>
      <view class="article-meta">
        <text class="article-author">作者: {{article.author}}</text>
        <text class="article-date">日期: {{article.date}}</text>
        <text class="article-category" wx:if="{{article.category}}">分类: {{article.category}}</text>
      </view>
      <view class="article-stats">
        <text class="stat-item">👁 {{article.viewCount || 0}}</text>
        <text class="stat-item">👍 {{article.likeCount || 0}}</text>
        <text class="stat-item">💬 {{article.commentCount || 0}}</text>
      </view>
    </view>
    
    <view class="article-content">
      <rich-text nodes="{{article.content}}"></rich-text>
    </view>
    
    <view class="article-actions">
      <button class="action-button" bindtap="copyArticleLink">
        <text class="button-icon">📋</text>
        <text>复制链接</text>
      </button>
      <button class="action-button" open-type="share">
        <text class="button-icon">📤</text>
        <text>分享</text>
      </button>
    </view>
  </block>
</view>