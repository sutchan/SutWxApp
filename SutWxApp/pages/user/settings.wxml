锘?!-- 閻劍鍩涚拋鍓х枂妞ょ敻娼?-->
<view class="settings-container">
  <!-- 娑擃亙姹夋穱鈩冧紖鐠佸墽鐤?-->
  <view class="section" wx:if="{{!isLoading && !error}}">
    <view class="section-header">
      <text class="section-title">娑擃亙姹夋穱鈩冧紖</text>
    </view>
    
    <view class="settings-list">
      <view 
        class="setting-item"
        bindtap="navigateToEditProfile"
        aria-label="缂傛牞绶稉顏冩眽鐠у嫭鏋?>
        <view class="setting-content">
          <text class="setting-label">缂傛牞绶稉顏冩眽鐠у嫭鏋?/text>
        </view>
        <image class="arrow-icon" src="/images/arrow-right.svg" mode="aspectFit"></image>
      </view>
      
      <view 
        class="setting-item"
        bindtap="navigateToBindAccount"
        aria-label="鐠愶箑褰跨紒鎴濈暰">
        <view class="setting-content">
          <text class="setting-label">鐠愶箑褰跨紒鎴濈暰</text>
          <text class="setting-value" wx:if="{{userInfo && userInfo.bound_to_wordpress}}">瀹歌尙绮︾€?/text>
          <text class="setting-value unbound" wx:else>閺堫亞绮︾€?/text>
        </view>
        <image class="arrow-icon" src="/images/arrow-right.svg" mode="aspectFit"></image>
      </view>
    </view>
  </view>

  <!-- 闁氨鐓＄拋鍓х枂 -->
  <view class="section" wx:if="{{!isLoading && !error}}">
    <view class="section-header">
      <text class="section-title">闁氨鐓＄拋鍓х枂</text>
    </view>
    
    <view class="settings-list">
      <view class="setting-item">
        <view class="setting-content">
          <text class="setting-label">閹恒劑鈧線鈧氨鐓?/text>
        </view>
        <switch 
          checked="{{notificationSettings.pushEnabled}}"
          bindchange="updateNotificationSetting"
          data-type="push"
          color="#1aad19">
        </switch>
      </view>
      
      <view class="setting-item">
        <view class="setting-content">
          <text class="setting-label">鐠囧嫯顔戦幓鎰板晪</text>
        </view>
        <switch 
          checked="{{notificationSettings.commentEnabled}}"
          bindchange="updateNotificationSetting"
          data-type="comment"
          color="#1aad19">
        </switch>
      </view>
      
      <view class="setting-item">
        <view class="setting-content">
          <text class="setting-label">缁崵绮哄☉鍫熶紖</text>
        </view>
        <switch 
          checked="{{notificationSettings.systemEnabled}}"
          bindchange="updateNotificationSetting"
          data-type="system"
          color="#1aad19">
        </switch>
      </view>
    </view>
  </view>

  <!-- 閸忔湹绮拋鍓х枂 -->
  <view class="section" wx:if="{{!isLoading && !error}}">
    <view class="settings-list">
      <view 
        class="setting-item"
        bindtap="navigateToPrivacySettings"
        aria-label="闂呮劗顫嗙拋鍓х枂">
        <view class="setting-content">
          <text class="setting-label">闂呮劗顫嗙拋鍓х枂</text>
        </view>
        <image class="arrow-icon" src="/images/arrow-right.svg" mode="aspectFit"></image>
      </view>
      
      <view 
        class="setting-item"
        bindtap="clearCache"
        aria-label="濞撳懘娅庣紓鎾崇摠">
        <view class="setting-content">
          <text class="setting-label">濞撳懘娅庣紓鎾崇摠</text>
        </view>
        <image class="arrow-icon" src="/images/arrow-right.svg" mode="aspectFit"></image>
      </view>
      
      <view 
        class="setting-item"
        bindtap="navigateToAbout"
        aria-label="閸忓厖绨幋鎴滄粦">
        <view class="setting-content">
          <text class="setting-label">閸忓厖绨幋鎴滄粦</text>
        </view>
        <image class="arrow-icon" src="/images/arrow-right.svg" mode="aspectFit"></image>
      </view>
    </view>
  </view>

  <!-- 閻楀牊婀版穱鈩冧紖 -->
  <view class="version-info" wx:if="{{!isLoading && !error}}">
    <text class="version-text">閻楀牊婀?1.0.3</text>
  </view>

  <!-- 閸旂姾娴囬悩鑸碘偓?-->
  <view class="loading-container" wx:if="isLoading">
    <view class="loading"></view>
    <text class="loading-text">閸旂姾娴囨稉?..</text>
  </view>

  <!-- 闁挎瑨顕ら幓鎰仛 -->
  <view class="error-container" wx:if="error">
    <image src="/images/error.svg" class="error-icon"></image>
    <text class="error-text">{{error}}</text>
    <button class="retry-button" bindtap="onRetry">闁插秷鐦?/button>
  </view>
</view>\n