/**
 * 文件名: pointsExchange.wxml
 * 版本号: 1.0.0
 * 更新日期: 2025-11-23
 * 描述: pointsExchange 椤甸潰缁撴瀯鏂囦欢
 */
<view class="container">
  <!-- 绉垎澶撮儴 -->
  <view class="points-header">
    <view class="points-info">
      <text class="points-label">鎴戠殑绉垎</text>
      <text class="points-value">{{currentPoints}}</text>
    </view>
    <view class="points-actions">
      <view class="action-item" bindtap="onGoToDetail">
        <text class="iconfont">绉垎鏄庣粏</text>
      </view>
      <view class="action-item" bindtap="onGoToTasks">
        <text class="iconfont">璧氱Н鍒?/text>
      </view>
    </view>
  </view>

  <!-- 鍒嗙被鏍囩 -->
  <view class="tab-bar">
    <view 
      wx:for="{{tabs}}" 
      wx:key="key" 
      class="tab-item {{activeTab === item.key ? 'active' : ''}}" 
      data-tab="{{item.key}}" 
      bindtap="onTabChange">
      {{item.label}}
    </view>
  </view>

  <!-- 鎺掑簭绛涢€?-->
  <view class="sort-bar">
    <view class="sort-current" bindtap="onShowFilter">
      <text>{{sortOptions.find(item => item.key === sortType).label}}</text>
      <text class="iconfont arrow">鈻?/text>
    </view>
  </view>

  <!-- 鍐呭鍖哄煙 -->
  <view class="content">
    <!-- 鍏戞崲鍟嗗搧鍒楄〃 -->
    <view class="exchange-list" wx:if="{{!isEmpty}}">
      <view 
        wx:for="{{exchangeList}}" 
        wx:key="id" 
        class="exchange-item">
        
        <!-- 鍟嗗搧鍥剧墖 -->
        <view class="item-image" bindtap="onViewDetail" data-id="{{item.id}}">
          <image src="{{item.image}}" mode="aspectFill" class="image"></image>
          <!-- 搴撳瓨涓嶈冻鏍囩 -->
          <view class="stock-tag" wx:if="{{item.stock <= 0}}">宸插厬瀹?/view>
          <!-- 鐑棬鏍囩 -->
          <view class="hot-tag" wx:if="{{item.isHot}}">鐑棬</view>
        </view>
        
        <!-- 产品信息 -->
        <view class="item-info">
          <view class="item-name" bindtap="onViewDetail" data-id="{{item.id}}">{{item.name}}</view>
          <view class="item-desc">{{item.description}}</view>
          
          <!-- 鍏戞崲淇℃伅 -->
          <view class="item-exchange">
            <view class="points-info">
              <text class="points-value">{{item.points}}</text>
              <text class="points-label">绉垎</text>
            </view>
            
            <!-- 搴撳瓨淇℃伅 -->
            <view class="stock-info" wx:if="{{item.stock > 0}}">
              <text class="stock-text">鍓╀綑{{item.stock}}浠?/text>
            </view>
            <view class="stock-info out-of-stock" wx:else>
              <text class="stock-text">宸插厬瀹?/text>
            </view>
          </view>
          
          <!-- 鎿嶄綔鎸夐挳 -->
          <view class="item-actions">
            <button 
              class="detail-btn" 
              bindtap="onViewDetail" 
              data-id="{{item.id}}">
              璇︽儏
            </button>
            <button 
              class="exchange-btn {{item.stock <= 0 || currentPoints < item.points ? 'disabled' : ''}}" 
              bindtap="onExchange" 
              data-id="{{item.id}}"
              disabled="{{item.stock <= 0 || currentPoints < item.points}}">
              {{item.stock <= 0 ? '宸插厬瀹? : (currentPoints < item.points ? '绉垎涓嶈冻' : '绔嬪嵆鍏戞崲')}}
            </button>
          </view>
        </view>
      </view>
    </view>

    <!-- 绌虹姸鎬?-->
    <view class="empty-state" wx:if="{{isEmpty}}">
      <image class="empty-icon" src="/images/empty-exchange.png" mode="aspectFit"></image>
      <text>鏆傛棤鍏戞崲鍟嗗搧</text>
      <view class="empty-tip">鏁鏈熷緟鏇村绮惧僵鍟嗗搧</view>
    </view>

    <!-- 加载状态-->
    <view class="loading-state" wx:if="{{isLoading && exchangeList.length === 0}}">
      <view class="loading-spinner"></view>
      <text>加载中..</text>
    </view>

    <!-- 娌℃湁鏇村鏁版嵁 -->
    <view class="no-more-data" wx:if="{{!hasMore && exchangeList.length > 0}}">
      娌℃湁鏇村鍟嗗搧浜?    </view>
  </view>

  <!-- 绛涢€夊脊绐?-->
  <view class="filter-popup" wx:if="{{showFilter}}" bindtap="onHideFilter">
    <view class="filter-content" catchtap="">
      <view class="filter-header">
        <text>鎺掑簭鏂瑰紡</text>
        <view class="close-btn" bindtap="onHideFilter">脳</view>
      </view>
      <view class="filter-options">
        <view 
          wx:for="{{sortOptions}}" 
          wx:key="key" 
          class="option-item {{sortType === item.key ? 'active' : ''}}" 
          data-sort="{{item.key}}" 
          bindtap="onSelectSort">
          {{item.label}}
        </view>
      </view>
    </view>
  </view>
</view>
