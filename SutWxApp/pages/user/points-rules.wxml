锘?!-- 缁夘垰鍨庣憴鍕灟妞ょ敻娼?-->
<view class="rules-container">
  <!-- 缁夘垰鍨庨懢宄板絿鐟欏嫬鍨?-->
  <view class="section" wx:if="{{!isLoading && !error && earnRules.length > 0}}">
    <view class="section-header">
      <text class="section-title">缁夘垰鍨庨懢宄板絿鐟欏嫬鍨?/text>
    </view>
    
    <view class="rules-list">
      <view 
        wx:for="{{earnRules}}" 
        wx:key="index"
        class="rule-item"
        aria-label="缁夘垰鍨庣憴鍕灟閿涙{item.description || ''}}">
        <view class="rule-content">
          <text class="rule-description">{{item.description || ''}}</text>
          <text class="rule-limit" wx:if="{{item.dailyLimit > 0}}">濮ｅ繑妫╂稉濠囨閿涙{item.dailyLimit}}濞?/text>
          <text class="rule-limit" wx:elif="{{item.dailyLimit === 1}}">娑撯偓濞嗏剝鈧冾殯閸?/text>
        </view>
      </view>
    </view>
  </view>
  
  <!-- 缁夘垰鍨庢担璺ㄦ暏鐟欏嫬鍨?-->
  <view class="section" wx:if="{{!isLoading && !error && useRules.length > 0}}">
    <view class="section-header">
      <text class="section-title">缁夘垰鍨庢担璺ㄦ暏鐟欏嫬鍨?/text>
    </view>
    
    <view class="rules-list">
      <view 
        wx:for="{{useRules}}" 
        wx:key="index"
        class="rule-item"
        aria-label="缁夘垰鍨庢担璺ㄦ暏鐟欏嫬鍨敍姝縶item.description || ''}}">
        <view class="rule-content">
          <text class="rule-description">{{item.description || ''}}</text>
          <text class="rule-limit" wx:if="{{item.minPoints > 0}}">閺堚偓娴ｅ簼濞囬悽顭掔窗{{item.minPoints}}缁夘垰鍨?/text>
        </view>
      </view>
    </view>
  </view>
  
  <!-- 缁夘垰鍨庢潻鍥ㄦ埂鐟欏嫬鍨?-->
  <view class="section" wx:if="{{!isLoading && !error && expireRules}}">
    <view class="section-header">
      <text class="section-title">缁夘垰鍨庢潻鍥ㄦ埂鐟欏嫬鍨?/text>
    </view>
    
    <view class="rules-list">
      <view class="rule-item" aria-label="缁夘垰鍨庢潻鍥ㄦ埂鐟欏嫬鍨敍姝縶expireRules.description || ''}}">
        <view class="rule-content">
          <text class="rule-description">{{expireRules.description || ''}}</text>
        </view>
      </view>
    </view>
  </view>

  <!-- 鐢瓕顫嗛梻顕€顣?-->
  <view class="section" wx:if="{{faqs.length > 0 && !isLoading && !error}}">
    <view class="section-header">
      <text class="section-title">鐢瓕顫嗛梻顕€顣?/text>
    </view>
    
    <view class="faq-list">
      <view 
        wx:for="{{faqs}}" 
        wx:key="index"
        class="faq-item"
        bindtap="toggleFaq"
        data-index="{{index}}"
        aria-label="鐢瓕顫嗛梻顕€顣介敍姝縶item.question || ''}}">
        <view class="faq-header">
          <text class="faq-question">{{item.question || '閺堫亜鎳￠崥宥夋６妫?}}</text>
          <image 
            class="faq-icon {{item.expanded ? 'expanded' : ''}}" 
            src="/images/arrow-down.svg" 
            mode="aspectFit">
          </image>
        </view>
        <view class="faq-answer" wx:if="{{item.expanded}}">
          <text>{{item.answer || '閺嗗倹妫ょ粵鏃€顢?}}</text>
        </view>
      </view>
    </view>
  </view>

  <!-- 閸旂姾娴囬悩鑸碘偓?-->
  <view class="loading-container" wx:if="isLoading">
    <view class="loading"></view>
    <text class="loading-text">閸旂姾娴囨稉?..</text>
  </view>

  <!-- 闁挎瑨顕ら幓鎰仛 -->
  <view class="error-container" wx:if="error">
    <image src="/images/error.svg" class="error-icon"></image>
    <text class="error-text">{{error}}</text>
    <button class="retry-button" bindtap="onRetry">闁插秷鐦?/button>
    <text class="error-note">瑜版挸澧犻弰鍓с仛閻ㄥ嫭妲告妯款吇鐟欏嫬鍨敍宀冾嚞闁插秷鐦懢宄板絿閺堚偓閺傛媽顫夐崚?/text>
  </view>
</view>\n