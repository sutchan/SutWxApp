/**
 * 文件名: methods.wxml
 * 版本号: 1.0.0
 * 更新日期: 2025-11-23
 * 描述: methods 椤甸潰缁撴瀯鏂囦欢
 */
<!-- 鏀粯鏂瑰紡閫夋嫨椤甸潰 -->
<view class="payment-methods-container">
  <!-- 璁㈠崟閲戦淇℃伅 -->
  <view class="order-amount">
    <text class="amount-label">鏀粯閲戦</text>
    <text class="amount-value">楼{{orderAmount}}</text>
  </view>
  
  <!-- 鍔犺浇鐘舵€?-->
  <view wx:if="{{loading}}" class="loading-container">
    <view class="loading-spinner"></view>
    <text class="loading-text">姝ｅ湪鍔犺浇鏀粯鏂瑰紡...</text>
  </view>
  
  <!-- 鏀粯鏂瑰紡鍒楄〃 -->
  <view wx:else class="payment-methods-list">
    <view 
      wx:for="{{paymentMethods}}" 
      wx:key="id" 
      class="payment-method-item {{!item.enabled ? 'disabled' : ''}} {{selectedMethod === item.id ? 'selected' : ''}}"
      data-method="{{item.id}}"
      bindtap="selectPaymentMethod"
    >
      <view class="method-icon">
        <image src="{{item.icon}}" mode="aspectFit"></image>
      </view>
      
      <view class="method-info">
        <view class="method-name">
          {{item.name}}
          <text wx:if="{{item.recommended}}" class="recommended-tag">鎺ㄨ崘</text>
        </view>
        <view class="method-desc">{{item.desc}}</view>
      </view>
      
      <view class="method-radio">
        <view class="radio-icon {{selectedMethod === item.id ? 'checked' : ''}}"></view>
      </view>
    </view>
  </view>
  
  <!-- 纭鏀粯鎸夐挳 -->
  <view class="payment-actions">
    <button 
      class="confirm-btn {{submitting ? 'submitting' : ''}}" 
      bindtap="confirmPayment"
      disabled="{{submitting || !selectedMethod}}"
    >
      {{submitting ? '鏀粯涓?..' : '纭鏀粯'}}
    </button>
  </view>
</view>