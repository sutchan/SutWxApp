# 2026-09-13 工作日志

## 检查项目开发进度（用户请求）
- 当前版本 **3.0.10**（`package.json` / `app.js globalData.version` / README 徽章 / CHANGELOG 首节 四处一致，`npm run check:version` 通过）。
- 分支 `dev`，与 `origin/main` 同步，工作区干净无未提交改动。
- 近 2 日（09-12 ~ 09-13）密集提交主线：
  - 3.0.8 原型目录归一（`/prototype/`）+ CI/CD 流水线（ci/release/miniprogram-deploy 三 workflow、check-version/check-config/deploy 脚本、dependabot）。
  - 3.0.9 按职责拆分超长源文件（utils/request.js、utils/monitor.js、productService/orderService、pages/product|address|cart、app.js 等）。
  - 3.0.10 将 ESLint 8 的 `.eslintrc.js` 替换为 ESLint 10 纯声明式扁平配置 `eslint.config.js`，修复 CI 门禁（恢复 0 error / 11 warning 基线）。
- 页面已实现 9 个目录：home / category / product / cart / order / address / user / settings / help。
- 规范体系 10 类全部完成（原空文档 testing/user-guide 已填，data/design 已新建）。
- 记忆文件已修正：版本号 3.0.8→3.0.10，并标注 IMPROVEMENTS_REPORT.md 部分结论已过时。

## 仍存在的主要未完成项（从记忆与代码推断，非本次新改动）
- 真实 WordPress 后端对接未做：productService/categoryService 仍 mock，baseUrl 占位。
- tabBar 8 图标全为同一占位图，待美术资源替换。
- 文章 HTML 渲染层未接（sanitizeHtml 已清洗，渲染层待补 rich-text/towxml/wxParse）。
- 仓库尚无 Tag；miniprogram-deploy 流水线未在 GitHub 真实跑过。
