# 2026-09-13 工作日志

## 检查项目开发进度（用户请求）
- 当前版本 **3.0.10**（`package.json` / `app.js globalData.version` / README 徽章 / CHANGELOG 首节 四处一致，`npm run check:version` 通过）。
- 分支 `dev`，与 `origin/main` 同步，工作区干净无未提交改动。
- 近 2 日（09-12 ~ 09-13）密集提交主线：
  - 3.0.8 原型目录归一（`/prototype/`）+ CI/CD 流水线（ci/release/miniprogram-deploy 三 workflow、check-version/check-config/deploy 脚本、dependabot）。
  - 3.0.9 按职责拆分超长源文件（utils/request.js、utils/monitor.js、productService/orderService、pages/product|address|cart、app.js 等）。
  - 3.0.10 将 ESLint 8 的 `.eslintrc.js` 替换为 ESLint 10 纯声明式扁平配置 `eslint.config.js`，修复 CI 门禁（恢复 0 error / 11 warning 基线）。
- 页面已实现 9 个目录：home / category / product / cart / order / address / user / settings / help。
- 规范体系 10 类全部完成（原空文档 testing/user-guide 已填，data/design 已新建）。
- 记忆文件已修正：版本号 3.0.8→3.0.10，并标注 IMPROVEMENTS_REPORT.md 部分结论已过时。

## 仍存在的主要未完成项（从记忆与代码推断，非本次新改动）
- 真实 WordPress 后端对接：productService（v3.0.6）+ categoryService（v3.0.11）均已支持 mock/woocommerce 切换、含 WC 映射与单测；但 baseUrl 仍占位、无真实后端联调。
- tabBar 8 图标：已由 scripts/generate-tabbar-icons.js 生成品牌线性 PNG（home/category/cart/user × 灰/绿，81×81）覆盖占位图（v3.0.12）。
- 文章 HTML 渲染层：已接入（v3.0.12）——utils/richtext.js 安全清洗（保留 img/a）+ models/post.js + postService（mock/woocommerce）+ pages/article/{list,detail}（rich-text 渲染）+ help 页入口。
- 仓库尚无 Tag；miniprogram-deploy 流水线未在 GitHub 真实跑过。

## 本次完成：文章渲染层 + tabBar 图标（v3.0.12）
- 新增 utils/richtext.js（sanitizeArticleHtml，保留安全 img/a，移除 script/事件/危险协议）、models/post.js、services/postService(.mock).js（mock/woocommerce 切换）、pages/article/{list,detail}（rich-text 渲染）、scripts/tabbar-icons/* + generate-tabbar-icons.js（PNG 光栅化）。
- app.json 分包注册文章两页；help 页增加「养护文章」入口。
- 版本 bump 3.0.11→3.0.12；check:version / check:config 通过；ESLint 新增文件 0 error（1 warning eqeqeq 与基线一致）；richtext 清洗逻辑 node 验证通过（移除 script/javascript:/onclick，保留 http(s) img/a）。

## 本次完成：分类服务 WooCommerce 对接（v3.0.11）
- 新建 `models/category.js`（mapWooCommerceCategory/mapWooCommerceCategories）、`services/categoryService.mock.js`；`categoryService.js` 由纯 mock（v3.0.0）升级为 mock/woocommerce 切换，对称 productService 范式。
- 版本 bump 3.0.10→3.0.11（package.json/app.js/README/CHANGELOG 同步），`npm run check:version` 通过。
- 单测 `__tests__/category.mapper.test.js`（7 用例，node 直接验证映射正确）；ESLint 新文件 0 error / 1 warning（eqeqeq，与基线一致）。
- 下一步可选：文章 HTML 渲染层接入、tabBar 品牌图标、真实 baseUrl 联调。
