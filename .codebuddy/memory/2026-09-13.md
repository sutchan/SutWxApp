# 2026-09-13

## 完善项目文档 + WordPress 插件开发计划（grill-me 技能）
- 经 grill-me 追问确认决策：商品=WooCommerce；v1=只读 MVP（文章/商品/分类/主题，免登录）；鉴权=wx.login→JWT 基建本期落地；订单/支付本期仅预留。
- 新建 `openspec/specs/backend/spec.md`（插件架构、REST 命名空间映射、v1 端点契约、主题配置、鉴权/订单预留、安全缓存部署）。
- 新建 `docs/wordpress-plugin/DEVELOPMENT_PLAN.md`（P0 脚手架→P1 只读 MVP→P2 主题配置→P3 鉴权→P4 联调→P5 订单支付预留）。
- 补全 `openspec/specs/api/spec.md`：新增「文章 API」「分类 API」契约 + 「端点与后端命名空间映射」说明；更新 `openspec/README.md`、`README.md` 索引；`CHANGELOG.md` 顶加 `[Unreleased]` 文档节。
- 注意：小程序代码实际在 `SutWxApp/SutWxApp/`（README 导入路径亦如此），services/models 在此子目录。

## 文档全量同步（第二轮）
- `architecture/spec.md`：外部依赖/后端形态对齐 `sutwx-app-api`（命名空间 `sutwx/v1`、`/api/*` rewrite 直连、mock/woocommerce 切换、v1 只读基线）；服务层补 `postService/themeService/dataSource`；部署注明插件随 WP 站点；版本历史加 3.0.2 行。
- `docs/PROJECT_OVERVIEW.md`：结构树对齐真实代码（补 `models/`、`behaviors/`、`pages/article/`、`postService/themeService/dataSource/richtext/text/api`、`monitor*.js`），后端说明链接插件规范/计划，新增「文章模块」「主题换肤」模块说明，相关链接与版本历史补充。
- 实际目录核实：`services/` 共 13 文件（含 4 个 `*.mock.js`），`utils/` 拆分极细（`request-*.js` 6 个、`monitor-*.js` 6 个、`richtext/text/api` 等）。

## 后端插件 P0 脚手架落地（v3.1.0，插件独立版本 0.1.0）
- 新增仓库根 `sutwx-app-api/`：入口 `sutwx-app-api.php`（`Requires Plugins: woocommerce`，常量 `SUTWX_API_REST_NS=sutwx/v1`、`SUTWX_API_THEME_OPTION=sutwx_theme`）、`includes/class-loader.php`（rest_api_init 路由 + `/api/*`→`index.php?rest_route=/sutwx/v1/$1` 重写 + 激活 flush + 默认主题 option）、`includes/class-rest-base.php`（`{code,message,data,timestamp,requestId}` 包络基类 + 分页规整 pageSize≤50）、`includes/rest/` 四控制器（product/post/category 占位、theme 返回真实 `{"presetId":"sut-green"}` 达成 P0 验收）、`readme.txt`（baseUrl 配置说明 + wp-json 直连备选）。
- 版本 bump：3.0.14 → 3.1.0（minor，新功能）；同步 package.json/app.js（globalData+头注释 3.0.13→3.1.0）/README 徽章/CHANGELOG 新节；DEVELOPMENT_PLAN.md P0 前 4 项勾选（仅剩"本地 WP+WC 测试环境"未完成）。check:version 通过。本机无 PHP，`php -l` 未跑，语法校验留待 WP 环境。
- 后续 P1：`mappings/map-{product,post,category}.php` + 6 端点真实回调（WC `wc_get_products` / WP_Query / `get_terms(product_cat)`）。

## 后端插件 P2 主题后台配置落地（v3.4.0，插件 0.3.0）
- 新增 `sutwx-app-api/includes/class-settings.php`：主题配置读写+校验中心，内置 `THEME_PRESETS`（5 套）/ `THEME_KEYS`（11 字段，与 `SutWxApp/models/theme.js` 严格对齐），提供 `get_theme_config`/`sanitize_custom`/`sanitize_theme_config`/`update_theme_config`，写入前强制校验 presetId 白名单 + 清洗 custom 颜色字段。
- 新增 `sutwx-app-api/admin/settings-page.php`：后台「设置 → 小程序设置」页（预设下拉 + 11 字段 `<input type="color">` 颜色选择器，nonce 防护），经 `Sutwx_Settings::update_theme_config` 落库 option `sutwx_theme`。
- `sanitize.php` 增 `sutwx_is_valid_color`/`sutwx_sanitize_color`；`class-rest-theme.php` 改用 `Sutwx_Settings::get_theme_config()`；`class-loader.php` 实现 `register_admin_menu`。
- 新增单测 `sutwx-app-api/tests/run-settings-tests.php`（无需 WP 环境）。**本机无 PHP，未能本地跑测试/语法校验**，靠人工审查保证。
- 版本 bump：3.3.0 → 3.4.0（minor，P2 功能）；同步 package.json/app.js(头注释+globalData)/README 徽章(顺带修正 3.3.0 时遗漏的陈旧 3.2.0)/CHANGELOG 新节；插件 0.2.0→0.3.0（入口+readme.txt Stable tag）；DEVELOPMENT_PLAN P2 勾选；backend/spec 版本历史加 0.3.0。check:version 通过，lint 仍 0 error/12 warning。
- **重要修正**：README 版本徽章此前停在 3.2.0（3.3.0 升级时漏改），本次一并修正为 3.4.0。后续 bump 版本须核查所有展示位。
