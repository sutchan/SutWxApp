# 2026-09-13

## 完善项目文档 + WordPress 插件开发计划（grill-me 技能）
- 经 grill-me 追问确认决策：商品=WooCommerce；v1=只读 MVP（文章/商品/分类/主题，免登录）；鉴权=wx.login→JWT 基建本期落地；订单/支付本期仅预留。
- 新建 `openspec/specs/backend/spec.md`（插件架构、REST 命名空间映射、v1 端点契约、主题配置、鉴权/订单预留、安全缓存部署）。
- 新建 `docs/wordpress-plugin/DEVELOPMENT_PLAN.md`（P0 脚手架→P1 只读 MVP→P2 主题配置→P3 鉴权→P4 联调→P5 订单支付预留）。
- 补全 `openspec/specs/api/spec.md`：新增「文章 API」「分类 API」契约 + 「端点与后端命名空间映射」说明；更新 `openspec/README.md`、`README.md` 索引；`CHANGELOG.md` 顶加 `[Unreleased]` 文档节。
- 注意：小程序代码实际在 `SutWxApp/SutWxApp/`（README 导入路径亦如此），services/models 在此子目录。
