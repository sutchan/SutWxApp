# 2026-09-12

## 创建 .github Community Health Files
在 `SutWxApp/.github/` 下新建了 GitHub 社区健康文件（中文，对齐项目约定）：
- `CODE_OF_CONDUCT.md`（Contributor Covenant 2.1）
- `CONTRIBUTING.md`（开发环境、分支策略 main/dev/feature/*/fix/*、Conventional Commits、SemVer 版本同步、PR 流程）
- `SECURITY.md`（仅支持最新版、私有漏洞报告、纯前端无后端密钥说明）
- `SUPPORT.md`
- `PULL_REQUEST_TEMPLATE.md`
- `ISSUE_TEMPLATE/config.yml`、`bug_report.yml`、`feature_request.yml`

注意：社区健康文件提交规范引用了仓库既有的 git 规则（Conventional Commits、SemVer 每次改动 bump 版本、更新 `SutWxApp/app.js` 的 `globalData.version` 与 `CHANGELOG.md`）。

## 项目后端实情澄清（重要）
用户明确：小程序**以 WordPress 网站为后端（headless CMS），在微信上优化排版并显示网站内容**。
- 当前活跃文档（project/architecture/README/PROJECT_OVERVIEW/api specs）只写"外部 REST API"，**未点明 WordPress**，描述不完整。
- 代码现状：`app.js` baseUrl 为占位 `https://api.example.com`；首页调自定义 `/api/banner|category|product/list`，`authService` 调 `/auth/*`（均非标准 WP REST `/wp-json/wp/v2`、`/wp-json/jwt-auth/v1/token`）；`productService`/`categoryService` 直接 return mock，未真正对接 WP。
- `utils/request.js` 有 `sanitizeHtml()`，可用于渲染 WP 文章 HTML。
- `openspec/项目概述.md`（旧文件，本轮未改）仍写"Node.js+Express+MySQL+Redis+MongoDB+积分+社交"，与 WordPress 内容展示定位矛盾，需修正/删除。
- 缺口排序：①未指明 WordPress ②内容渲染/排版优化零文档 ③旧项目概述矛盾 ④鉴权与内容模型未说明。

## 文档完善已落地（v3.0.3 → 3.0.4）
已补全并同步版本至 3.0.4：
- `project/spec.md`：项目背景/定位改为 WordPress 内容展示；技术栈说明 + 后端说明点明 WordPress；模块划分加「已实现 vs 规划」标注
- `architecture/spec.md`：新增「WordPress 后端与内容渲染」章节（headless CMS 形态 + 内容渲染与排版优化核心能力：HTML→WXML、`sanitizeHtml`、排版规范、图片优化）
- `data/spec.md`：目的说明 + 新增「WordPress 内容实体映射」表
- `api/spec.md`：补充 WordPress 后端与 `/api/*`↔`/wp-json/wp/v2/` 说明
- `README.md` / `docs/PROJECT_OVERVIEW.md`：简介、快速开始、技术栈补充 WordPress 后端；README 徽章 3.0.4
- `openspec/项目概述.md`：修正虚构技术栈（Node/Express/MySQL）与项目背景，测试策略去 Supertest/Postman
- `app.js`/`package.json` 版本号、CHANGELOG 新增 3.0.4 节
注意：文档按"当前接口以 `/api/*` 暴露（由 WP 插件或反向代理桥接 WP REST）"描述；代码实际 `productService`/`categoryService` 仍 return mock、baseUrl 占位，文档未声称已真对接，仅标注实现状态。

## 参考项目接入（v3.0.4 → 3.0.5）
用户提供 WordPress 对接参考：微慕 Minapper/Watch-Life（`iamxjb/winxin-app-watch-life.net`）+ 插件 `rest-api-to-miniprogram`。
- 真实形态：插件在 WP 注册**自定义 REST 命名空间**（`wp-json/minapper/v1/`、`wp-json/watch-life-net/v1/`），非裸 `wp-json/wp/v2/`；鉴权走微信 `wx.login` code→插件签发 token（非纯 JWT）；文章 HTML 用 `wxParse` 渲染。
- 已据该参考校正架构/API 规范表述（裸 wp/v2→自定义命名空间；JWT→微信 code token 或 JWT），README 加参考链接，版本升 3.0.5，CHANGELOG 补 [3.0.5]。
- 待用户决定：是否按参考启动真实 WP 对接（需可用 WP 站点+插件）与补文章 HTML 渲染层（rich-text/towxml/wxParse）。

## WooCommerce 商品支持（v3.0.5 → 3.0.6）
用户要求小程序支持 WordPress WooCommerce 插件并在小程序显示商品信息。
- 新增 `SutWxApp/models/product.js`：`mapWooCommerceProduct`/`mapWooCommerceProducts` 将 WooCommerce 商品映射为统一 DTO（幂等规整，补缺 specs）。
- `services/productService.js` 加数据源开关 `globalData.productSource`（`mock`/`woocommerce`）；woocommerce 路径调 `/api/product/list`、`/api/product/detail`（插件映射 `wp-json/wc/v3/products`）。
- 详情页 `pages/product/index.js` 改为经 `productService.getProductDetail` 获取并映射；mock 商品补 `specs`。
- 新增单测 `__tests__/product.mapper.test.js`（node 直跑已验证：去标签/多规格/容错正常；jest 因无 node_modules 未能在本地跑）。
- 规范同步：`data`/`api`/`features`/`architecture`/`user-guide` spec 补 WooCommerce 章节；版本 3.0.6，CHANGELOG 补 [3.0.6]。

## 主题换肤（v3.0.6 → 3.0.7）
用户要求：WordPress 后端可设置小程序主题，且小程序自带几种默认配色预设。已落地**全站动态换肤** + 约定 `/api/theme`：
- 新增 `SutWxApp/models/theme.js`：内置 5 套默认配色预设（`THEME_PRESETS`：sut-green/sky-blue/sunny-orange/violet/graphite）；`resolveTheme({presetId, custom})` 合并后台自定义十六进制色值；`toCssVars` 生成 CSS 变量声明字符串（字段对应 `app.wxss` 的 `--primary-color` 等）。
- 新增 `SutWxApp/services/themeService.js`：`loadAndApplyTheme(app)` 从 `/api/theme` 拉取（缓存优先、失败回退默认），写入 `globalData.theme`/`themeStyle`，并通过 `wx.setNavigationBarColor` + `wx.setTabBarStyle` 下发，同时 `setData` 给当前已渲染页面。
- 新增 `SutWxApp/behaviors/theme.js`：页面 Behavior，`onLoad`/`onShow` 从 `globalData.themeStyle` 读取注入 `data.themeStyle`。
- `app.js`：引入 `themeService`，`globalData` 加 `theme`/`themeStyle`（版本升 3.0.7），`initApp` 调 `this.loadTheme()`。
- 全站 11 个页面根容器绑定 `style="{{themeStyle}}"`（unescaped 页面用 `<view class="container" ...>`，category/cart/order 等实体转义 wxml 仅替换 `class="X"` 片段避免 `<` 歧义），JS 注册 `behaviors: [themeBehavior]`。
- 单测 `__tests__/theme.mapper.test.js`（9 用例，全过）；`api/spec.md` 增「主题接口」、`features/spec.md` 增「2.5 主题换肤」；README 补说明与结构；CHANGELOG 补 [3.0.7]。
- 注意：category/cart/order 等 wxml 是 HTML 实体转义存储（`&lt;view&gt;`，WXML 解析时解码），编辑必须用属性片段精确匹配。

## 原型文件归位至 /prototype（版本 3.0.2 → 3.0.3）

按用户要求「/prototype 为项目原型目录，移入所有原型文件，精简项目结构」执行：

- 新建仓库根 `prototype/`，原型文件统一存放为 `prototype/prototype.html`（`git mv` 保留历史）。
- 删除根目录 `prototype.html`（508 字节的重定向占位页，指向旧路径，已无必要）。
- 删除空的 `openspec/prototype/` 目录，规范目录职责更单一。
- `README.md` 原型链接更新为 `prototype/prototype.html`，版本徽章升至 3.0.3。
- `CHANGELOG.md` 顶部新增 `[3.0.3] - 2026-09-12` 小节（历史条目中的旧路径保留不改）。
- 版本同步：`SutWxApp/package.json` 3.0.3、`SutWxApp/app.js` 头注释与 `globalData.version` 3.0.3。

重要发现：执行移动期间，`openspec/prototype/prototype.html` 被外部（并行会话/用户）替换为**新版原型**（1696 行、v3.1.0 设计系统、带文件头注释，90148 字节），而 HEAD 中的旧版为 2703 行 Apple Style 版本（80949 字节）。最终以新版内容作为 `prototype/prototype.html`。教训：**动文件前必须实查磁盘真实内容与 mtime，不能按快照/索引版本执行移动**。

## 原型审查（用户「审查原型」→ 二次发现已被外部大幅升级）
用户要求审查原型。二次读取发现 `prototype/prototype.html` 已被并行会话改写至**设计系统 v3.1.1 + v3.2 动效**（新增渐变 token、骨架屏、Banner Ken Burns、加购飞入购物车、收藏涟漪、`prefers-reduced-motion`、13 商品、脱敏评价数据、全程「演示数据」标注），并新增 `prototype-extra.html`、`wireframes.html` 两个兄弟页。
- 上一轮提的 P2（版本号 v1.0.18 混淆）、P3（购物车步进无库存上限）均已被外部改写修复。
- 仍存在的问题（已记入 MEMORY.md 原型一节，待用户决定是否修）：
  - **P2 商品详情图轮播不可用**：`pdDots` 无 onclick、无自动播放/滑动，`goDetailSlide` 仅初始化调用一次，与首页 banner 可交互轮播不一致。
  - **P2 `phImg()` 内联 SVG 渐变 `id="g"` 重复**：同页多图 → `url(#g)` 文档级重复解析，全部串成第一张渐变色（按 seed 的绿阶失效）；`avSvg()` 已用唯一 `avg<seed>` 但 `phImg` 没改。
  - P3：文件头注释「单文件自包含/无外部依赖」因新增两个兄弟页已轻微过时；可点击元素仍 `<div onclick>` 缺 role/tabindex/aria（`prefers-reduced-motion` 已覆盖，视觉原型可接受）；`confirmSpec(event)` 多余参数无害。
- 已向用户交付结构化审查结论，并询问是否修复两个 P2（绑定 `pdDots` 点击 + 给 `phImg` 渐变加唯一 id）。用户尚未回复是否执行。

## 执行原型修复（用户「执行修复」）
用户确认执行。实施前复核发现：②`phImg()` 所有 9 处调用均走 `<img src="data:...svg">`，SVG 在图片内隔离解析，`url(#g)` 不跨图串色——**原"渐变 id 重复导致串色"为误报，不改**。仅修 ①商品详情图轮播：
- `renderProduct` 的 `pdDots` 改为 4 个 `<i onclick="goDetailSlide(k);startDetailAuto()">`（1636 行）。
- 新增 `detailTimer`+`startDetailAuto()`/`stopDetailAuto()`（1713-1722 行）：进入商品页 3.5s 自动轮播，`prefers-reduced-motion` 时停。
- `setPage` 增加 `if (key==='product') startDetailAuto(); else stopDetailAuto();`（1449 行），离开商品页即停。
- 版本：仅 bump 原型自身构建版本至 v3.1.2（文件头注释 5 行 + 页脚 `version-tip` 1183 行），设计规范 v3.1.1 与小程序版本 3.0.4 未动。
- 已读取 1636/1449/1713-1722 行验证改动正确落地。

## 完善 CI/CD（版本 3.0.7 → 3.0.8）

用户要求「完善项目的 CI/CD」。此前仓库只有 `.github/ISSUE_TEMPLATE/` 与社区健康文件，**无任何 workflows**；且 `CHANGELOG.md` 已有 `[3.0.8]` 小节但 `package.json`/`app.js`/README 徽章仍停在 3.0.7（版本脱节）——本次把这 3.0.8 补齐并追加 CI/CD 变更。

新增文件：
- `.github/workflows/ci.yml`：push（main/dev/feature/fix/hotfix）、PR、手动触发；jobs = `lint`（npm ci + eslint）、`test`（`npm run test:coverage`，覆盖率 Artifact 归档 7 天）、`checks`（纯 Node 校验，无需装依赖）。`defaults.run.working-directory: SutWxApp`，Node 20 + npm 缓存（cache-dependency-path 指向 SutWxApp/package-lock.json），concurrency 取消同分支旧运行。
- `.github/workflows/release.yml`：推送 `v*.*.*` Tag（或手动指定 tag）→ 先校验 Tag 与版本号一致，再抽 `CHANGELOG.md` 小节创建 GitHub Release（softprops/action-gh-release@v2），首版无 Tags。
- `.github/workflows/miniprogram-deploy.yml`：`workflow_dispatch`（choice: preview/upload）；写私钥到 `$RUNNER_TEMP` → `npm install --no-save miniprogram-ci@^2` → `node scripts/deploy.js`；结束清理私钥；Secret 为 `MINIPROGRAM_PRIVATE_KEY`（必填）+ `MINIPROGRAM_APPID`（project.config.json 仍为占位 `touristappid` 时必填）。
- `.github/dependabot.yml`：npm（`/SutWxApp`）+ github-actions，每周一 09:00 Asia/Shanghai。
- `SutWxApp/scripts/check-version.js`（比对 package.json / app.js globalData.version / README 徽章 `badge/version-x.y.z` / CHANGELOG 首节，支持 RELEASE_TAG 校验 Tag）。
- `SutWxApp/scripts/check-config.js`（JSON 可解析、页面 js+wxml 必需/json+wxss 警告、tabBar 图标存在且 ≤40KB、sitemap 存在、未被 app.json 引用的页面告警）。
- `SutWxApp/scripts/deploy.js`（miniprogram-ci 预览/上传，忽略清单与 packOptions 一致）。
- 仓库根 `scripts/changelog-section.js`（按版本抽取 CHANGELOG 小节，供 Release body_path）。

关键修复与治理：
- **真实缺陷**：`app.json` tabBar 引用的 `images/tabbar/cart.png`、`cart-active.png` **根本不存在**（6 个 tabbar 图标 MD5 完全相同，均为占位图）→ 用 home.png 复制补齐，保持与现有占位图一致。
- **ESLint 门禁原为红**：`npm run lint` 会话初实测 **138 error**（此前会话记录的"0 错误"已失效）→ 修复后 **0 error / 11 warning**（warning 全为 `eqeqeq` 松比较，属 dataset 字符串场景，保留）。修法：`__tests__` 加 `env.jest`；globals 补 `Behavior`/`getCurrentPages`/`window`/`requestAnimationFrame`；`utils/compress-images.js` 单独按 Node ESM 处理并加入上传忽略；清理未使用变量（`app`/`that`/`request`/`addressId`/订单回调 `id`）与失效 `@ts-nocheck` 注释。
- `package.json`：版本 3.0.8，新增 `check`/`ci`/`test:coverage`/`deploy:preview`/`deploy:upload`，`engines.node>=18`。
- `project.config.json`：`packOptions.ignore` 忽略 `__tests__`/`scripts`/`node_modules`/`coverage`/`package*.json`/`.eslintrc.js`/`utils/compress-images.js`。
- `.gitignore`：新增私钥（`private.*.key` 等）、`SutWxApp/preview.jpg`、`coverage/`。
- 文档：README 加 CI 徽章 + 「持续集成与交付」章节；`.github/CONTRIBUTING.md`（v1.0.1）加 CI/CD 说明与 PR 清单；`openspec/specs/ops/spec.md`（3.0.8）新增「CI/CD 流水线」并更新版本来源/发布检查清单；`openspec/specs/testing/spec.md`（1.0.1）把过时的 Bun CI 示例改为真实 Node 20 + Jest 流水线；`openspec/specs/development/spec.md` CI/CD 章节对齐现实（无蓝绿/滚动部署）。

验证结果（本地实测）：`npm run lint` 0 error/11 warning；`npm test` 5 suites/32 tests 通过；`npm run test:coverage` 语句覆盖率 82.17%；`npm run check` 通过（18 个 JSON、11 页面、4 项 tabBar 图标）；4 个 YAML 经 js-yaml 解析通过；`changelog-section.js v3.0.8` 正常输出。

其它观察：本次会话期间存在**自动提交机制**（出现 3 个自动 commit：`ci: 新增 CI/CD 流水线与版本配置校验`、`chore: 清理未使用代码并更新 lint 配置`、`chore: 清零 ESLint 错误并启用代码检查门禁`），其中误把我在排查时产生的 `SutWxApp/ci-*.txt`（4 个空/日志文件）一并提交，已删除并确认 HEAD 中不再存在。另有仓库根 `/tests` 目录（.ts/.js）已被 `.gitignore` 的 `/tests` 忽略，属本地遗留、不参与 CI。

