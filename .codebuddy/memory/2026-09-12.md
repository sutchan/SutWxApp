# 2026-09-12 工作记录

## 源代码按职责拆分（单文件 ≤200 行规则落地）
对 SutWxApp 小程序 8 个超 200 行源文件完成拆分，版本 bump 3.0.8 → 3.0.9。

### 拆分清单
- `utils/request.js`(738) → `request-config` / `request-platform`(与 monitor 共用 getWx/checkWx) / `request-cache` / `request-security` / `request-security-sql`(SQL 注入校验) / `request-cancel` / `request-api`(含 handleResponse) / `request`(主)。
- `utils/monitor.js`(557) → `monitor-config` / `monitor-utils` / `monitor-core` / `monitor-report`(error/performance/behavior) / `monitor-observers`(mark/measure/路由) / `monitor-scheduler`(自动上报定时器) / `monitor-collectors`(网络/FPS/内存/页面/API) / `monitor`(主)。注意 networkType/isOnline 状态归 core，采集器经 `core.setNetworkStatus` 回写；性能/错误/行为上报经 `monitor-report`。
- `services/productService.js` / `services/orderService.js` 演示数据 → `*.mock.js`。
- `pages/product/index.js`(410) loading/交互分离 → `behaviors/product-loaders.js` + `behaviors/product-actions.js`（主页面 `behaviors` 注入，导出契约不变）。
- `pages/address/index.js` → `validators.js` / `format.js` / `handlers.js`。
- `pages/cart/index.js` → `calc.js`（金额/数量纯函数 `buildCartState`）。
- `app.js`(239) → `helpers.js` + `helpers-auth.js`(登录/带鉴权请求，自包含不依赖 helpers)。

### 验证结果（拆分后）
- ESLint：0 error / 11 warning（warning 全为 `eqeqeq` 松比较，dataset 字符串场景的已知基线，故意保留）。
- Jest：`npm test` 5 suites / 32 tests 全通过。
- `npm run check:version`：package.json / app.js globalData.version / README 徽章 / CHANGELOG 顶部小节 全部 = 3.0.9。
- 行数统计：SutWxApp 下全源文件 ≤200 行（0 个超限）。

### 注意事项
- 拆分仅按职责下沉，未改变任何公开导出契约（request 函数对象方法、monitor 对象键、Page/Behavior 配置、App 方法均保持不变）。
- 头注释版本规范：仅改动/新增文件写 `// 版本号: 3.0.9`，未全仓库批量刷写（遵循用户规则）。旧文件头注释仍停留 3.0.0~3.0.7，属历史脱节，未批量修正。
